extends ../layout

block content
  h2= title
  p 
    | Are you sure you want to delete #{book.title} by #{book.author}? 

  form(action="/books/" + book.id + "/delete", method="post")
    a(href="/books/" + book.id) No! Cancel! 
    | 
    input(type="submit", value = "Yes, I'm sure")